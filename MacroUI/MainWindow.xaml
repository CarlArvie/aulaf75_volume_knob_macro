<Window x:Class="MacroUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MacroUI"
        mc:Ignorable="d"
        Title="MainWindow" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" WindowState="Maximized">
    <Grid Background="#01000000"> <!-- Slight background so it can receive events if needed, though mostly we don't need clicks, just transparent overlay -->
        <Canvas x:Name="MainCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" Width="800" Height="800" RenderTransformOrigin="0.5,0.5">
            <Canvas.RenderTransform>
                <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
            </Canvas.RenderTransform>
        </Canvas>
    </Grid>
</Window>
